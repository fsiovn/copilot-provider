# Create an extension

1. Create an extension

    ```bash
    npm install --global yo generator-code

    yo code
    ```

    - *What type of extension do you want to create?*

        - `New Extension (TypeScript)`

    - *What's the name of your extension?*
    
        - `Copilot Provider`

    - *What's the identifier of your extension?*
    
        - `copilot-provider`

    - *What's the description of your extension?*
    
        - `Provider for GitHub Copilot`

    - *Enable JavaScript type checking in 'jsconfig.json'?*
    
        - `No`

    - *Initialize a git repository?*
    
        - `Yes`

    - *Which package manager to use?*

        - `npm`

    ```console
    ? What type of extension do you want to create?
    New Extension (TypeScript)
    ❯ New Extension (JavaScript)
    New Color Theme
    New Language Support
    New Code Snippets
    New Keymap
    New Extension Pack
    New Language Pack (Localization)
    New Web Extension (TypeScript)
    New Notebook Renderer (TypeScript)
    ```

    ```console
    ? What's the name of your extension? Copilot Provider
    ```

    ```console
    ? What's the identifier of your extension? copilot-provider
    ```

    ```console
    ? What's the description of your extension? Provider for GitHub Copilot
    ```

    ```console
    ? Enable JavaScript type checking in 'jsconfig.json'? No
    ```

    ```console
    ? Initialize a git repository? Yes
    ```

    ```console
    ? Which package manager to use?
    ❯ npm
    yarn
    pnpm
    ```

    ```bash
    mv ./copilot-provider/* ./
    mv ./copilot-provider/.* ./
    ```

2. Install all dependencies

    ```bash
    npm install
    ```
